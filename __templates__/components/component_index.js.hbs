import * as React from 'react';

import { I{{ pascalCase name }} } from './data';
import { {{ pascalCase name }} as Layout } from './Layout';

export const {{ pascalCase name }} = (props: I{{ pascalCase name }} ) => <Layout {...props} />;